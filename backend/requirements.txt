fastapi==0.111.*
uvicorn[standard]==0.29.*
pydantic==2.7.*
pydantic-settings==2.2.* # for config.py
python-dotenv==1.0.*
httpx==0.27.*            # async HTTP client
celery==5.3.*
redis==5.0.*
python-jose[cryptography]==3.3.*  # JWT validation
sentry-sdk==2.3.* # unused now will do app tracking in future
ultralytics          # YOLOv8n — phone detection (COCO class 67)
opencv-python-headless  # frame decode — headless, no GUI deps
mediapipe            # face detection — optimised for video call faces
websockets           # async WebSocket client for RTMS stream
supabase==2.16.0